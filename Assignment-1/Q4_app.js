// This question uses ES Modules. To execute, "type": "module" must be added in package.json or run in a separate environment.”
import {count,increment} from "./Q4_counter.js";
console.log(count); //0
increment();
console.log(count); //1



